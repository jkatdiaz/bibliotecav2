import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.app",
  appName: "Biblioteca",
  webDir: "dist",
  // server: {
  //   // Permitir navegación a todas las subrutas bajo deuscaritasestf.org
  //   allowNavigation: ["deuscaritasestf.org", "*.deuscaritasestf.org"],
  // },
};

export default config;
